name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

# Use the sparse registry protocol so each crate is fetched as an
# individual HTTPS request rather than via a single huge git clone of
# the index.  This is far more resilient to transient network issues
# on Windows runners (which occasionally fail with "Connection was
# reset" while pulling crates.io).
env:
  CARGO_NET_GIT_FETCH_WITH_CLI: "true"
  CARGO_NET_RETRY: "10"
  CARGO_HTTP_MULTIPLEXING: "false"
  CARGO_REGISTRIES_CRATES_IO_PROTOCOL: "sparse"

jobs:
  build:
    name: Build (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: macos-13
            target: x86_64-apple-darwin
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: windows-latest
            target: x86_64-pc-windows-msvc

    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo registry and build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install Linux deps
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libxkbcommon-dev libwayland-dev

      - name: Fetch dependencies (with retry)
        shell: bash
        run: |
          for i in 1 2 3 4 5; do
            cargo fetch --target ${{ matrix.target }} && break
            echo "cargo fetch failed (attempt $i), sleeping 15s..."
            sleep 15
          done

      - name: Build release
        run: cargo build --release --offline --target ${{ matrix.target }}

      - name: Strip (non-Windows)
        if: runner.os != 'Windows'
        run: strip target/${{ matrix.target }}/release/engvis-cli

      - name: Package
        shell: bash
        run: |
          mkdir -p artifacts
          cd target/${{ matrix.target }}/release
          if [ "$RUNNER_OS" = "Windows" ]; then
            7z a ../../../artifacts/engvis-cli-${{ matrix.target }}.zip engvis-cli.exe
            cp engvis-cli.exe ../../../artifacts/engvis-cli-${{ matrix.target }}.exe
          elif [ "$RUNNER_OS" = "macOS" ]; then
            chmod +x engvis-cli
            mkdir -p dmg_staging
            cp engvis-cli dmg_staging/
            hdiutil create -volname engvis-cli -srcfolder dmg_staging \
              -ov -format UDZO ../../../artifacts/engvis-cli-${{ matrix.target }}.dmg
            tar czf ../../../artifacts/engvis-cli-${{ matrix.target }}.tar.gz engvis-cli
          else
            chmod +x engvis-cli
            tar czf ../../../artifacts/engvis-cli-${{ matrix.target }}.tar.gz engvis-cli
            DEB_VERSION=$(echo "${{ github.ref_name }}" | sed 's/^v//')
            mkdir -p deb_staging/usr/bin deb_staging/DEBIAN
            cp engvis-cli deb_staging/usr/bin/
            printf 'Package: engvis-cli\nVersion: %s\nSection: graphics\nPriority: optional\nArchitecture: amd64\nMaintainer: nil\nDescription: Engineering visualization framework with GPU rendering\n' "${DEB_VERSION}" > deb_staging/DEBIAN/control
            dpkg-deb --build deb_staging ../../../artifacts/engvis-cli-${{ matrix.target }}.deb
          fi

      - name: Upload to release
        uses: softprops/action-gh-release@v3
        with:
          files: artifacts/*
          fail_on_unmatched_files: true
